//! Backup executor
//! TODO: Migrate from kalamdb-core/src/jobs/executors/backup.rs

use crate::error::Result;

pub struct BackupExecutor {
    // TODO: Add fields during migration
}

impl BackupExecutor {
    pub fn new() -> Result<Self> {
        Ok(Self {})
    }
}
