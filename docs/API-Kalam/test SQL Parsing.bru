meta {
  name: test SQL Parsing
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/sql
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
  "sql": "CREATE TABLE events (id BIGINT, name TEXT)"
  }
}
